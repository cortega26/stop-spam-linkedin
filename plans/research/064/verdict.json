{"buildScope":["options/options.js: generalize testerFindMatch into a private explainTesterText helper (same precedence, same live-read storage path) plus render branches for the new kinds.","options/options.html: one static testerScopeNote hint line; no live-region structural change.","_locales/en/messages.json + _locales/es/messages.json: new keys from design §2 only, keeping $N placeholder parity.","tests/extension-interactions.js: regressions modeled on the probe pairs near line 1800 — one probe per verdict kind plus truncation and would-match probes."],"caseCount":14,"checks":[{"command":"npm run smoke","result":"exit 0 (jq validation + en/es parity + node --check)"},{"command":"npm run lint","result":"exit 0 (eslint; worktree resolves modules via a gitignored node_modules symlink to the main checkout's installed toolchain — no install, no main-checkout writes)"},{"command":"npm run typecheck","result":"exit 0 (tsc --noEmit)"},{"command":"npm run test:unit","result":"exit 0 — 81 pass, 0 fail"},{"command":"node --test plans/research/064/prototype.test.cjs","result":"exit 0 — 15 pass, 0 fail (precedence, all verdict/disabled kinds, cap, no-mutation, fixture conformance, matcher parity)"},{"command":"node -e cases.json gate (plan step 1)","result":"exit 0 — 14 cases, all 5 kinds, all 4 disabled-rule reason codes"},{"command":"git diff --name-only fcbafd4 -- . ':!plans'","result":"empty (no production files changed)"}],"helperPlacement":"private in options/options.js. content.js never needs explanations, so promoting the helper to shared/pattern-data.js would add browser globals, tsconfig types, ESLint load-surface entries, and manifest plus popup/options script-tag ordering concerns for zero live-matching benefit. Sharing would additionally require keeping background.js's private copies untouched per the plan-048 constraint. No new storage keys, permissions, or network requests in either placement.","integrationScenarios":["read-failure: chrome.storage.sync.get error or missing keys renders testerError; must never render testerNoMatch (build-time browser case, not a pure-matcher guarantee).","storage-update-between-clicks: keep runTester's live sync read on every click; build the explanation from that same read so a change in another tab is reflected immediately.","truncation: inputs over 5000 chars test the prefix only and always append the testerTruncated note (reason code stays would-match when a disabled rule trips the prefix).","feed/tester probe pairs: extend the tests/extension-interactions.js pairs near line 1800 so each verdict kind asserts the same text in the tester and the feed; packaged Chromium/Firefox gates stay required for the eventual build."],"measuredBehavior":["Prototype effective decisions match the current testerFindMatch precedence on all 12 non-error fixtures (parity test); the only deltas are the more specific explanation (wouldMatch notes, empty-input error).","Exclusion, allow, custom-first attribution, disabled-language, disabled-pattern, and disabled-custom semantics each proven by a dedicated assertion using the real shared builders and real built-in regexes.","Inputs frozen plus JSON snapshot prove explainText never mutates settings; repeated calls return deep-equal results.","No production file changed: git diff against branch-start fcbafd4 outside plans/ is empty."],"productionFiles":["options/options.js","options/options.html","_locales/en/messages.json","_locales/es/messages.json","tests/extension-interactions.js"],"remainingUnknowns":["Exact rendered wording after translator review of the ES drafts (placeholders already parity-safe by construction).","Whether the would-match note should deep-link to the rule's row in options (UX detail for the build; no semantic impact).","Browser-only proof for the read-failure render path and the aria-live announcement order (requires the Playwright gates at build time)."],"verdict":"proceed"}