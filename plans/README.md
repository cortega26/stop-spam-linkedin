# Implementation Plans

Generated by the `improve` skill (deep audit) on 2026-08-08, against commit
`1f7f4e3`. A follow-up deep audit pass on 2026-08-14 (same commit — zero
drift) reconciled the queue and added plans 013–019. Execute in the order
below unless dependencies say otherwise. Each executor: read the plan fully
before starting, honor its STOP conditions, and update your row when done.

**Archival rule (standing)**: the moment a plan's status row is set to
DONE **or** REJECTED, its file MUST be moved to `plans/archive/` (e.g.
`git mv plans/005-unit-test-coverage.md plans/archive/`) and the index
link updated to `archive/<file>`. DONE/REJECTED = done, archived, out of
the active queue. BLOCKED and TODO plans stay in `plans/`. Never delete
a plan file — the archive is the record.

This audit read every source file directly (content.js, background.js,
popup/, options/, i18n.js, manifest.json, the test suite, and the packaging
scripts — roughly 2,800 lines total) rather than fanning out subagents,
since the codebase is small enough to fit in full context. Two of the
findings below (the storage-quota cap and the snooze-resume bug) were
verified empirically — byte-exact quota math for #001, direct code tracing
for #002 — rather than estimated.

Plans 007–012 came from a follow-up `next`/direction pass (2026-08-08, same
commit) surveying feature/roadmap opportunities. Two of them (007, 009) are
implementation-ready; three (008, 010, 011) are **design plans** — the
deeper pass that produced them found real architectural decisions
(a page-scanning strategy that doesn't exist yet, a multi-function API
change, a data-shape addition) that shouldn't be pre-baked into
line-numbered steps against code that other plans will change first. One
(012) is a **process plan** — adding a new detection language requires
native-speaker validation this session isn't positioned to fabricate, so
it documents the process rather than shipping unverified regex patterns.

## Direction findings (feature ideas surveyed)

Six feature ideas were surveyed and presented to the maintainer, who
confirmed all six as net-positive and consistent with the extension's
local-only, no-network-requests positioning. All six became plans:

1. **Manage "Not spam" exclusions in the options page** → plan 007. Real
   gap: `ss_excluded` had no management UI at all, unlike the whitelist.
2. **"Always block this author" list** → plan 008 (design). Mirrors the
   existing whitelist conceptually, but needs a page-scanning strategy
   that doesn't exist today — see that plan's "core design tension."
3. **Full settings backup/restore** → plan 009. Export/Import currently
   covers only phrases, not whitelist/exclusions/language toggles.
4. **Show which pattern/phrase triggered each block** → plan 010 (design).
   Also nets a simplification: deletes a block of duplicated
   pattern-rebuilding logic in `blockPost()`.
5. **Per-pattern (not just per-language) built-in toggle** → plan 011
   (design, blocked on 004).
6. **Additional detection languages** → plan 012 (process plan — see why
   this one isn't a code-change plan in its own file).

A seventh idea — shared/remote pattern sync or a community blocklist —
was considered and explicitly rejected; see "Findings considered and
rejected" below.

### Follow-up pass (2026-08-14): new plans 013–019

The second audit found two fresh direction ideas, both confirmed net-positive
against the extension's local-only positioning, and both became
implementation-ready plans:

1. **"Show all hidden posts" popup button** → plan 018. The machinery
   (`restoreBlocked()`) already exists and is wired to disable/snooze but
   no single user-facing action; one message case + one button exposes it.
2. **"Report missed spam" placeholder button** → plan 019. Copies the
   matched post text to the clipboard (user-initiated only — nothing is
   sent by the extension) and opens the pre-filled `missed_spam_pattern`
   GitHub issue form, closing the README's issue-form feedback loop.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | [Phrase storage quota](archive/001-phrase-storage-quota.md) | P1 | M | — | DONE (2026-08-14, branch advisor/001-phrase-storage-quota @ 575eb6b, reviewed + verified) |
| 002 | [Snooze resume bug](archive/002-snooze-resume-bug.md) | P1 | S | — | DONE (2026-08-14, branch advisor/002-snooze-resume-bug @ ffa2eff, reviewed + verified; regression test added to tests/extension-smoke.js) |
| 003 | [Toggle double-count](archive/003-toggle-double-count.md) | P2 | S | — (soft: touches code near 002) | DONE (2026-08-14, branch advisor/003-toggle-double-count @ 5f4fb21, reviewed + verified; regression test in tests/extension-smoke.js) |
| 004 | [Shared pattern data](archive/004-shared-pattern-data.md) | P2 | M | — | DONE (2026-08-14, branch advisor/004-shared-pattern-data @ 68f18d2, reviewed + verified; 10 regexes/10 labels byte-identical) |
| 005 | [Unit test coverage](archive/005-unit-test-coverage.md) | P2 | M | 004 | DONE (2026-08-14, branch advisor/005-unit-test-coverage @ 48e882e, reviewed + verified; 11/11 unit tests; test:unit script uses glob form for Node 24) |
| 006 | [ESLint setup](archive/006-eslint-setup.md) | P3 | S | — | DONE (2026-08-14, branch advisor/006-eslint-setup @ 3a02af3, reviewed + verified; 72 findings resolved via config, zero source edits) |
| 007 | [Exclusion management UI](archive/007-exclusion-management-ui.md) | P2 | M | — (soft: 001) | DONE (2026-08-14, branch advisor/007-exclusion-management-ui @ 6eb4024, reviewed + verified; migration e2e: 100 legacy entries preserved) |
| 008 | [Author blocklist (design)](archive/008-author-blocklist.md) | P3 | M | — (soft: 005) | DONE (2026-08-14, branch advisor/008-author-blocklist @ 0928f28, reviewed + verified; blockPost info-param merged with 010; 2 real e2e bugs caught+fixed during execution) |
| 009 | [Full settings backup/restore](archive/009-full-settings-backup.md) | P2 | M | — (soft: 007) | DONE (2026-08-14, branch advisor/009-full-settings-backup @ 3aba455, reviewed + verified; 5 import/export e2e scenarios) |
| 010 | [Match attribution (design)](archive/010-blocked-by-attribution.md) | P3 | M | 004 | DONE (2026-08-14, branch advisor/010-blocked-by-attribution @ f65f5b6, reviewed + verified; overlap-case e2e green) |
| 011 | [Per-pattern toggle (design)](archive/011-per-pattern-toggle.md) | P3 | M | 004 | DONE (2026-08-14, branch advisor/011-per-pattern-toggle @ 447c6ee, reviewed + verified; 10 stable pattern ids, per-pattern-disable e2e green) |
| 012 | [New-language process](archive/012-additional-languages-process.md) | P3 | S–unbounded | — | REJECTED (2026-08-15 — maintainer decision: the Phase 2 validation gate (fluent native speaker or a 10–15×2 real-example corpus) can't be sourced, so no new language can pass it; the process doc stays as the reference for any future language request) |
| 013 | [Show cooldown is real](archive/013-show-cooldown.md) | P2 | S | 004, 005 | DONE (2026-08-14, branch advisor/013-show-cooldown @ 550e7df, reviewed + verified; 5 unit tests + e2e scenario; Step 4 reworked via reload for determinism) |
| 014 | [e2e interactive flows](archive/014-e2e-interactive-flows.md) | P2 | M | — | DONE (2026-08-14, branch advisor/014-e2e-interactions @ 912826d, reviewed + verified; 14 interaction scenarios; unblocked by 020) |
| 015 | [Shared constants](archive/015-shared-constants.md) | P3 | M | — (soft: 001) | DONE (2026-08-14, branch advisor/015-shared-constants @ 22e6dc7, reviewed + verified) |
| 016 | [Repo AGENTS.md](archive/016-agents-md.md) | P2 | S | — | DONE (2026-08-14, branch advisor/016-agents-md @ e4a4c73, reviewed + verified) |
| 017 | [CI Playwright cache](archive/017-ci-playwright-cache.md) | P3 | S | — | DONE (2026-08-14, branch advisor/017-ci-playwright-cache @ cd5ebe9, reviewed + verified; plan revised to ci.yml-only — release job never ran Playwright) |
| 018 | [Show-all restore](archive/018-show-all-restore.md) | P2 | S | — | DONE (2026-08-14, branch advisor/018-show-all-restore @ f4fc7de, reviewed + verified) |
| 019 | [Report missed spam](archive/019-report-missed-spam.md) | P2 | S | — | DONE (2026-08-14, branch advisor/019-report-missed-spam @ bd93853, reviewed + verified) |
| 020 | [blockPost idempotent](archive/020-blockpost-idempotent.md) | P1 | S | — | DONE (2026-08-14, branch advisor/020-blockpost-idempotent @ eca005b, reviewed + verified; toggle-on duplicate-placeholder bug fixed, strict assertion proven live) |
| 022 | [Exclusion eviction scoring](archive/022-exclusion-eviction-scoring.md) | P1 | S–M | — | DONE (2026-08-15, branch advisor/022-exclusion-eviction-scoring @ 3fc5dcc in /tmp/opencode/wt-022, reviewed + verified; 40 unit tests; e2e green) |
| 023 | [Whitelist same-tab restore](archive/023-whitelist-same-tab-restore.md) | P1 | S | — | DONE (2026-08-15, branch advisor/023-whitelist-same-tab-restore @ ffee372 in /tmp/opencode/wt-023, reviewed + verified; regression proven — test fails on unfixed code via stash round-trip; 40 unit tests; e2e green) |
| 024 | [parseAuthorId URIError](archive/024-parse-author-id-uri.md) | P1 | S | — | DONE (2026-08-15, branch advisor/024-parse-author-id-uri @ c54b97b in /tmp/opencode/wt-024, reviewed + verified; malformed → null no-throw, valid slugs unchanged; 42 unit tests) |
| 025 | [FR/PT/DE detection coverage + fixes](archive/025-fr-pt-de-detection.md) | P1 | M | — | DONE (2026-08-15, branch advisor/025-fr-pt-de-detection @ b174852 in /tmp/opencode/wt-025, reviewed + verified; FR-2/FR-1/PT-1 fixed, 54 unit tests incl. 12 new FR/PT/DE, e2e per-language green) |
| 026 | [Import byte quota](archive/026-import-quota.md) | P2 | S | — (soft: 022) | DONE (2026-08-15, branch advisor/026-import-quota @ e3c5451 in /tmp/opencode/wt-026, reviewed + verified; shared pruner on import, toast truthfulness, oversized-import e2e; write-failure revert mirrors phrase-save, not e2e-forced) |
| 027 | [Backup completeness](archive/027-backup-completeness.md) | P2 | S | — (soft: 026) | DONE (2026-08-15, branch advisor/027-backup-completeness @ 3437d64 in /tmp/opencode/wt-027, reviewed + verified; blockedAuthors/disabledPatterns/hide toggles round-trip e2e + legacy compat; 2 new locale keys) |
| 028 | [restoreBlocked lastBlocked](archive/028-restoreblocked-lastblocked.md) | P2 | S | — | DONE (2026-08-15, branch advisor/028-restoreblocked-lastblocked @ b6336d3 in /tmp/opencode/wt-028, reviewed + verified; regression proven — reverted-fix run fails at new assertion; 54 unit tests; e2e green) |
| 029 | [UTF-8 byte quota](029-utf8-byte-quota.md) | P2 | S | — (soft: 031) | TODO |
| 030 | [Options edit preservation](030-options-edit-preservation.md) | P2 | M | — | TODO |
| 031 | [Storage error checks](031-storage-error-checks.md) | P2 | S–M | — (soft: 029) | TODO |
| 032 | [restorePost pruning](032-restorepost-pruning.md) | P2 | S | — (soft: 023) | TODO |
| 033 | [Cooldown eviction order](033-cooldown-eviction.md) | P3 | S | — | TODO |
| 034 | [buildPatterns extraction](034-buildpatterns-extraction.md) | P2 | M | 025 | TODO |
| 035 | [Stats pipeline e2e](035-stats-e2e.md) | P2 | S–M | — | TODO |
| 036 | [Firefox smoke negatives](036-firefox-negatives.md) | P3 | S | — | TODO |
| 037 | [AGENTS.md refresh](037-agents-md-refresh.md) | P3 | S | — | TODO |
| 038 | [Smoke coverage](038-smoke-coverage.md) | P3 | S | — | TODO |
| 039 | [Context menus cleanup](039-contextmenus-cleanup.md) | P3 | S | — | TODO |
| 040 | [Block-author button](040-block-author-button.md) | P2 | S–M | — | TODO |
| 041 | [Per-pattern stats (design)](041-per-pattern-stats-design.md) | P3 | M | — (soft: 022) | TODO |
| 042 | [Missed-spam language](042-missed-spam-language.md) | P2 | S | — (soft: 034) | TODO |
| 043 | [Suggestion loop (design)](043-suggestion-loop-design.md) | P3 | M | — | TODO |

Status values: TODO | IN PROGRESS | DONE (→ move the file to `plans/archive/` and relink the index row) | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- **005 requires 004**: plan 005 extends the `shared/pattern-data.js` file
  that plan 004 creates, adding `escapeRegex`, `isLinkedInHost`,
  `parseAuthorId`, `hashString`, and `getExcludedSignature` exports to it.
  Do not attempt 005 before 004 has landed.
- **002 and 003 are independent but touch adjacent code**: both edit
  `content.js` near the toggle/`restoreBlocked()` logic — 002 fixes a
  correctness bug in `restoreBlocked()` itself, 003 fixes a separate
  correctness bug in `blockPost()`'s counting, and 003's plan explicitly
  notes it must re-read `restoreBlocked()`'s live state if 002 already
  landed. Either order works; just don't run them concurrently on the same
  branch without rebasing between them.
- **006 is independent of everything else** but its `eslint.config.js`
  `files` globs anticipate `shared/**/*.js` and `tests/unit/**/*.js` from
  004/005. If 006 runs first, those globs simply won't match anything yet
  (harmless); if it runs after, double-check the globs still line up with
  the paths 004/005 actually used.
- **001 is fully independent** of the other five — it touches
  `content.js`/`background.js`/`options/options.js` write paths that don't
  overlap with 002/003's toggle/snooze/counting logic or 004/005's pattern
  data extraction, beyond all four sharing `content.js` as a file (normal
  merge-conflict risk, not a logical dependency).
- **007 soft-interacts with 001**: both touch `ss_excluded`'s quota
  exemption note in 001's Scope section. Either order works — 001's Scope
  section already documents how to check 007's status before assuming the
  exemption still holds.
- **009 soft-interacts with 007**: 009's `excluded` import/export handling
  works with either the pre-007 (bare hash strings) or post-007
  (`{sig, preview, created}` objects) shape, so it doesn't hard-depend on
  007 — but running 007 first produces a more useful export (previews
  travel with the backup). Either order works.
- **008 soft-benefits from 005**: it calls `parseAuthorId`/`isLinkedInHost`,
  which are easier to reuse correctly (and test) if 005 already extracted
  them to `shared/pattern-data.js`. Not a hard dependency — `background.js`
  can maintain its own copy if 005 hasn't landed, following this repo's
  existing small-duplication convention.
- **010 hard-depends on 004** for clean label sourcing (see that plan's
  "Decision 1") — marked BLOCKED rather than TODO even though a fallback
  path without 004 exists, because the fallback recreates exactly the kind
  of hand-duplicated data 004 exists to eliminate. Wait for 004.
- **011 hard-depends on 004** — it adds a stable `id` field directly to
  the file 004 creates (`shared/pattern-data.js`) and has no meaningful
  path without it. Marked BLOCKED.
- **010 and 011 both touch pattern-matching internals** if both are
  implemented — check each other's status before starting either if both
  are in flight, to avoid conflicting edits to `buildPatterns`/the matching
  path.
- **008 and 010 both extend `blockPost()`'s parameter list** (008 adds a
  `context` object for author-blocklist-triggered blocks; 010 adds
  `matchInfo` for attribution). Whichever lands second should merge the two
  additions into one coherent parameter rather than landing them as two
  independent, conflicting signature changes — both plans' "Maintenance
  notes" flag this explicitly.
- **012 is independent** of every other plan, mechanically — it only adds
  data following an existing shape. Its real cost is the validation process
  (Phase 1/2), not code review of the eventual diff.
- **013 hard-depends on 004 and 005**: it adds a `createCooldownStore`
  export to `shared/pattern-data.js` (created by 004) and its unit tests
  live in `tests/unit/cooldown-store.test.js` reusing the `test:unit`
  script and CI step that 005 creates. Do not start 013 before both have
  landed. 013's e2e scenario (Show → node re-creation → still visible)
  belongs to `tests/extension-smoke.js`, which always exists.
- **014 (e2e interactions) is independent** but deliberately excludes two
  scenarios owned elsewhere: the snooze-RESUME DOM assertion (plan 002's
  test plan — it fails on unfixed code) and the Show-button re-created-node
  case (013's scenario). It extracts a shared harness from
  `tests/extension-smoke.js`; 013's smoke-scenario addition and 014's
  harness extraction can land in either order (merge-safe, same file).
- **015 soft-depends on 001**: both edit `MAX_CUSTOM_PHRASES` and friends
  in `content.js`/`background.js`/`options.js`. Recommended order: 001
  first, then 015 extracts whatever 001 left into `shared/constants.js` —
  plan 001's maintenance notes carry the cross-reference. 015 also needs
  the `shared/` directory from 004 (or creates it itself if 004 hasn't
  landed) and 006's eslint globs already cover `shared/**/*.js`.
- **016 (AGENTS.md) is independent** and safe any time; it documents
  facts (including `test:unit` from 005 and `shared/constants.js` from
  015) with conditional phrasing so it stays accurate whichever order the
  queue runs in.
- **017 (CI cache) is independent**; it only edits workflow files.
- **018 and 019 are independent features** touching popup/content/locales;
  both add their e2e scenarios to `tests/extension-smoke.js`, so if they
  run together on one branch, keep the scenarios ordered and
  non-overlapping. 018's popup driving is also exercised by 014's
  interactions file — no dependency, just shared fixtures.
- **019's "Report" button adds a button to the placeholder** that 014's
  scenario file counts or queries — 014 asserts on `[data-ss-ph]` counts
  and button labels, so a new button is additive and safe either order.

### Batch 022 (2026-08-15, deep audit at `ca9905f`) — dependency notes

- **034 requires 025**: plan 034 extracts `buildPatterns` to the shared
  module; plan 025's corpus tests (landing first) pin the pattern
  behavior the extraction must preserve byte-for-byte. Do not run 034
  before 025.
- **026 soft-depends on 022**: 026 reuses `SS_pruneExcludedByBytes`
  (created by 022) for the import path; if 022 hasn't landed, 026
  inlines the same byte math (its plan documents both branches).
- **027 soft-depends on 026**: both touch the import merge loop in
  `options/options.js`; run 026 first so the error discipline exists
  before adding more merge fields.
- **029 and 031 are soft-coupled**: 029 changes the byte measurement at
  the same call sites 031 hardens; run 029 first (measurement), then
  031 (error reporting).
- **032 soft-depends on 023**: 023 fixes the whitelist-restore behavior
  (also in `content.js` near `restorePost`); run 023 first, then 032's
  cleanup.
- **042 soft-depends on 034**: 042 threads the pattern id into the
  report payload; 034 changes where pattern ids come from — either
  order works, but 034 first keeps the id vocabulary stable.
- **037 is deliberately LAST**: it documents facts other plans change
  (smoke coverage per 038, storage architecture which stays stable
  through 022-036). Write it after the batch, not before.
- **041 soft-depends on 022**: the stats shape change touches the same
  storage area as the pruner extraction; sequencing avoids churn, not
  a hard conflict.
- **040 and 023 are adjacent**: 040 adds a block-author button that
  writes `ss_blocked_authors`; 023's onChanged diff only reads
  whitelist — no conflict, but both touch the placeholder/restore
  neighborhood of `content.js`.

## Recommended execution order rationale

001 and 002 first — both are user-visible correctness bugs with verified
root causes, and both are self-contained (no dependencies). 003 next
(same file area as 002, lower severity). 004 before 005 (hard dependency).
Then 013 (needs 004+005 landed — it makes the "Show" cooldown real and
removes dead code). 007 and 009 (feature plans, implementation-ready) can
run any time after 001 — 007 first if you want 009's export to include
preview text. 014 (e2e interactions) and 018/019 (new features) are
independent and safe to slot anywhere after 002; 016 (AGENTS.md) early is
nice-to-have for executor ergonomics. 015 after 001 (see dependency
notes); 006 (lint) and 012 (new-language process) are independent and safe
to defer indefinitely; 017 (CI cache) any time. 010 and 011 stay BLOCKED
until 004 lands, then either order works. 008 is independent but benefits
from 005 landing first.

## Findings considered and rejected

- **Wrong-author whitelisting via an oversized post-container heuristic**:
  hypothesized that `findBySiblingHeuristic` could occasionally pick a
  container spanning multiple posts, causing "Never block this author" to
  whitelist the wrong profile. Plausible in principle, but verifying it
  requires live-DOM evidence (actual LinkedIn markup triggering the
  heuristic to over-select) that this audit didn't have access to. Not
  planned; if a user report ever surfaces this concretely, re-audit with
  the specific DOM structure that triggered it.
- **`forEachTextNode`'s second `SHOW_ELEMENT` TreeWalker for shadow-DOM
  discovery** (`content.js`, inside `forEachTextNode`): runs unconditionally
  over the full subtree on every scan just to find `shadowRoot`s, even
  though LinkedIn's feed markup rarely uses shadow DOM in practice. Real but
  minor overhead; investigate-level, not planned. Revisit if a future
  profiling pass shows scan latency as an actual user-visible problem.
- **Locale key coverage** (`t()` calls / `__MSG_*__` tokens vs.
  `_locales/en/messages.json`): verified via `grep`+`comm` diff — every key
  referenced in JS and both HTML files exists in the locale file, and vice
  versa (the handful of keys that appeared "unused" were false positives
  from a regex that didn't match `countMessage()`'s dynamic key arguments).
  No gap found; not planned.
- **`options/options.html` element IDs vs. `options.js` `getElementById`
  calls**: verified every ID options.js looks up exists in the HTML. No
  crash risk found; not planned.
- **Multi-tab counter race** (`content.js:796-799`): two LinkedIn tabs can
  overwrite each other's `blockedCount`/`dailyCounts` since each content
  script keeps independent in-memory state. Already documented in the code
  with an explicit comment; the maintainer has already accepted this as
  low-impact/cosmetic. Not re-planned here — see 003 for a related but
  distinct stat-accuracy fix (toggle-triggered double-counting), which is
  in scope because it's a single-tab, single-session bug rather than a
  cross-tab race.
- **ReDoS in the built-in detection patterns** (`content.js`
  `BASE_PATTERNS`, now `shared/pattern-data.js` after plan 004): reviewed
  every pattern for catastrophic-backtracking shapes (nested quantifiers
  like `(a+)+`). None found — all patterns are effectively linear in input
  length. Not planned.
- **Shared/remote pattern sync or community blocklist** (surfaced during
  the `next`/direction pass that produced plans 007–012): a natural-seeming
  extension of the phrase/pattern system — let users pull in
  community-curated phrase lists, or sync detection improvements across
  installs. Deliberately not planned, and shouldn't be without a product
  decision to change course first: it directly contradicts this
  extension's core positioning (README: "no analytics, telemetry, remote
  blocklists, AI APIs, or network requests of any kind"; badges for
  "Local only" / "No telemetry"). Recorded here so it isn't independently
  reinvented and proposed again without that tension being surfaced.

### Added by the 2026-08-14 follow-up pass

- **Popup undo index race** (`content.js` `undoBlock` + `popup.js`'s
  index-based rows): a new block between render and click could shift
  `lastBlocked` and undo the wrong post. Real but narrow — the undo window
  holds at most 5 entries and the shift requires a block landing in the
  same second as a click. Not planned.
- **Options-page whitelist additions don't un-hide already-blocked posts**:
  adding an author to the whitelist in options leaves their currently
  hidden posts hidden until reload or manual "Show". The in-flow "Never
  block this author" button already restores immediately; impact is
  limited to a rare edit-after-the-fact path. Not planned.
- **UTC-based "today" stats bucket**: `getTodayKey()` and the popup both
  use `toISOString()` UTC days, so the "today" boundary sits at UTC
  midnight rather than the user's local midnight. Internally consistent
  (both sides agree), purely cosmetic. Not planned.
- **`isCustom` pattern-rebuilding duplication in `blockPost()`**
  (`content.js:692-707` recompiles per-post regexes identical to
  `buildPatterns`'s logic): already folded into plan 010's scope ("deletes
  a block of duplicated pattern-rebuilding logic"); a standalone fix would
  double that work. Not separately planned.
- **Starter-pack exact phrases amplify false positives** ("COMMENT",
  "PDF", "10x" as whole-word exact matches will hide legitimate posts
  containing those words): deliberate, opt-in product behavior with a
  built-in undo path. Not planned.
- **`MAX_EXCLUSIONS` FIFO silent eviction** (oldest 100 exclusions drop
  off, silently re-blocking previously-excluded texts): plan 007's
  exclusion-management UI makes this visible and manageable. Not
  separately planned.
- **Dead `showCooldowns` cooldown in `content.js`**: NOT rejected — this
  became plan 013 (removes the dead branch and implements the documented
  15-minute protection keyed by post identity). Recorded here so no future
  audit reports it as new.

## Notes for future audit runs

- This was a `deep`-effort audit but conducted by direct reading (no
  subagent fan-out) given the repo's size (~2,800 lines). If the codebase
  grows substantially before the next `/improve` run, switch to the
  subagent-per-category approach the skill defaults to for larger repos.
- The storage-quota finding (001) and the snooze-resume finding (002) were
  both verified empirically rather than estimated — see each plan's
  "Current state" section for the exact method (byte-math computation for
  001, since a live browser wasn't available in this environment without
  installing Playwright's Chromium, which the audit role isn't permitted to
  do; direct code-path tracing for 002). If either plan's executor finds
  the live behavior doesn't match what's documented, that's a stronger
  signal of codebase drift than a normal STOP condition — investigate
  before dismissing.

## Execution record (2026-08-14) — all plans implemented except 012

### Follow-up work items (2026-08-15)

- **DONE — jsdom heuristic tests**: extracted `findPostContainer`/`findBySiblingHeuristic`/`findByKnownSelectors` to `shared/post-container.js` (UMD, byte-identical logic, verified programmatically) and added 8 unit tests with jsdom (dev-only dep). Merged to main as `bf19ad1`.
- **DONE — checkJs type checking**: `tsconfig.json` (`checkJs`/`allowJs`/`noEmit`), `@types/chrome`, `types/globals.d.ts`, JSDoc on the shared modules, `npm run typecheck` + CI step. tsc caught 3 real findings (removed DOM arg `expandEntityReferences` in `createTreeWalker`, a `shadowRoot` access on a `Node`, a number→string coercion). TypeScript pinned to 5.9.3 — TS7 (tsgo) removed `moduleResolution: node` which the config requires. Merged as `4ccdc75`. No build step introduced; shipped code unchanged.
- **DONE — Node 24.19.0 (latest LTS "Krypton")** installed via nvm as the default alias; project verified green on it (CI auto-uses latest 24.x — no workflow change needed).
- **BLOCKED (toolchain) — Firefox e2e via Playwright**: the profile `extensions/<gecko-id>/` staging mechanism does NOT load the unpacked extension in Playwright's bundled Firefox (Juggler build): the add-on manager initializes (11 Mozilla system add-ons register) but the staged extension never registers, even with `xpinstall.signatures.required=false` and `extensions.enabledScopes=15`, across two boots. `playwright-core` (1.60.0) contains no Firefox extension-loading code path. Root cause: Playwright's Firefox build does not support unpacked extension loading — a toolchain limitation, not an extension bug. Viable alternatives (not attempted — different toolchain): geckodriver's WebDriver API `POST /session/{id}/moz/addon/install` (base64 ZIP, temporary install) or `web-ext` for manual verification. If Firefox e2e is wanted later, scope it as its own effort with geckodriver.

## Execution record (2026-08-14) — all plans implemented except 012

Every plan except 012 was executed by a dispatched executor in an isolated
git worktree, reviewed by the advisor (re-run done criteria, scope audit,
full diff read), and verified against the full test suite (smoke + unit +
e2e unpacked + e2e packaged). All `advisor/*` branches were subsequently
merged to `main` (see `git log` merge commits; status rows above carry the
branch + commit). Plan 012 was REJECTED on 2026-08-15 (maintainer
decision — validation gate not sourceable); see its status row.

New findings surfaced during execution (not previously planned):

- **ES-1 pattern `\b` quirk (latent detection bug, discovered during plan
  011)**: in `shared/pattern-data.js` ES-1, the `env\u00ed|enviar\u00e9|dar\u00e9`
  alternatives are followed by `\b`, which can never match in a non-unicode
  regex (accented chars are non-word, so no boundary exists after them).
  Result: the most common Spanish bait forms ("comenta X y te envío el
  PDF", "...enviaré...", "...daré...") never match ES-1; only
  `comparto|mando|doy|regalo` alternatives fire. Fix sketch: drop the `\b`
  or replace with `(?!\w)` lookahead in ES-1 only. Not planned — it's a
  sensitive detection-pattern change with false-positive risk; the
  maintainer should weigh it. Verification was one-liner: `node -e
  "console.log(/(?:comenta)[^]*?enviar\u00e9\b/i.test('comenta X y te
  enviaré el PDF'))"` → false.
- **Toggle-on duplicate placeholder (fixed by plan 020)**: three separate
  executors (003, 013, 014) independently hit the double-scan race before
  it was characterized and fixed in 020. The 003/013-era loose assertions
  were tightened to exact counts afterward.
- **`release.yml` never runs Playwright** (plan 017 premise correction):
  the release `package` job is smoke+package only; the CI cache plan was
  scoped to `ci.yml` accordingly.
- **`node --test tests/unit/` directory form is broken on Node 24** (plan
  005): the `test:unit` script uses the glob form `node --test
  "tests/unit/*.test.js"` — documented in plan 005's command table.

## Execution record (2026-08-15, batch 021) — quick wins + Firefox e2e

A prioritization pass on the post-audit state (all plans merged, ES-1 fix
landed, no open issues) picked six items. All shipped on
`advisor/021-batch-quickwins-firefox-e2e`:

1. **fix(stats)**: `SS_getLocalDayKey` in shared/pattern-data.js is now the
   single day-boundary source; content.js + popup.js stats use local
   midnight instead of UTC (`toISOString`). popup.html now loads
   shared/pattern-data.js; 3 new unit tests. (Previously "not planned —
   cosmetic" in the 2026-08-14 pass; reconsidered as a user-visible
   correctness fix for non-UTC users.)
2. **fix(undo)**: `lastBlocked` entries carry a stable id (post data-id,
   else session uid) and `undoBlock` resolves by id, not array index —
   a new block between popup render and click can no longer restore the
   wrong post. e2e regression injects a third block after render and
   asserts the clicked row restores its own post. (Was "narrow, not
   planned"; 5-line fix, deterministic e2e.)
3. **fix(whitelist)**: whitelist additions via options/import un-hide that
   author's already-blocked posts (`restoreAuthorPosts`, driven from the
   onChanged diff); new `labelBlockedPosts` set keeps Promoted/Featured
   class-hides untouched. e2e: block authored spam → whitelist → restored,
   unrelated post stays hidden.
4. **feat(options)**: exclusions section shows entry count and a red
   near-cap warning computed with the same byte math as content.js's
   `pruneExcludedByBytes` (90% of QUOTA_BYTES_PER_ITEM). New i18n keys
   `excludedCountOne/Many`, `excludedNearCap` (en + es); e2e asserts the
   label. Silencing the silent FIFO-by-utility eviction from the audit.
5. **test(firefox)**: `tests/firefox-smoke.js` — geckodriver WebDriver
   harness. Installs the packaged zip as a temporary addon, serves the
   mock feed over HTTPS with a CONNECT proxy mapping www.linkedin.com:443
   to a local self-signed server (content scripts only match real LinkedIn
   origins), asserts spam is blocked and the addon registered with the
   addon manager (UUID in profile prefs — geckodriver hard-blocks
   navigation to moz-extension://). geckodriver added as devDependency;
   `npm run test:firefox`; CI step after test:package. This closes the
   "Firefox e2e BLOCKED (toolchain)" finding from the 2026-08-15 record:
   the Playwright/Juggler path remains unsupported, but the geckodriver
   route documented as an alternative is now implemented and green.
6. **docs(plans)**: this record — execution state corrected (branches ARE
   merged), batch recorded, load-surface facts in AGENTS.md fixed
   (popup.html loads pattern-data.js; content_scripts array shape).

## Execution record (2026-08-15, batch 022) — deep audit, plans 022–043

Third audit pass (deep, 6 parallel subagents + full manual vetting of
every finding's evidence) against commit `ca9905f` on
`advisor/021-batch-quickwins-firefox-e2e` (batch 021 unmerged at audit
time). All 22 plans were authored by the advisor; **none executed yet** —
status rows above are TODO.

Vetting corrections applied after subagent reports (evidence personally
re-opened and verified):

- **Eviction-scoring inversion (022) is real**: `Date.now()` ≈ 1.78e12 in
  2026 > the `1e12` tier constant, inverting the documented
  preview-less-first policy. Verified by reading `content.js:1306-1313`
  and both entry shapes in `normalizeExcludedEntries`.
- **Whitelist same-tab restore (023) is real and ships in batch 021**:
  self-writers mutate `whitelistedAuthors` in place before
  `storage.sync.set`, so the onChanged diff against the live set skips
  `restoreAuthorPosts` in the writing tab. Introduced by `a73deac`;
  the batch-021 e2e drives whitelist changes cross-context and misses it.
- **NEW detection bugs discovered during vetting (025)**: running real
  FR/PT/DE bait sentences against the patterns exposed that FR-2 is
  broken (missing `\s+` between the preposition group and verb group —
  "commentez MOT pour recevoir le guide" never matches), FR-1 misses
  object pronouns ("je vous enverrai"), and PT-1 misses the near-future
  ("vou enviar"). EN spot-checked solid. These elevate plan 025 from
  test-coverage to detection-bug fix; the audit subagents had flagged
  only the coverage gap.
- **Rejected at vetting**: SEC-01 `.env` plaintext (gitignored +
  untracked + never committed; standard local convention — recorded in
  the rejected list so it isn't re-audited); PERF-01 shadow-DOM
  TreeWalker (deferred in the 2026-08-14 record pending profiling
  evidence; no profiling done since); multi-tab counter race (accepted
  by design).

Direction findings delivered as plans: 040 (block-author button, build),
042 (missed-spam language, build), 041 and 043 (per-pattern stats and
suggestion loop, **design/spike** plans per the playbook — they deliver
a design document + throwaway prototype branch, not shipped code).

## Findings considered and rejected (batch 022)

- **SEC-01 — store-publish credentials in `.env`**: `.env` is gitignored
  and untracked (`.gitignore:14`), never committed (`git log --all --
  .env` empty), and no committed file embeds values — only env-var names
  in `RELEASE_CHECKLIST.md` / `release.yml` / `submit-stores.js`. The
  residual risk is local-disk access, which is the standard convention
  for local credential stores; the only improvement (mode-600 + a
  `.env.example`) is DX, not security. Not planned.
- **PERF-01 — unconditional second TreeWalker for shadow-DOM discovery**:
  real constant-factor cost, but the 2026-08-14 record deferred it
  pending profiling evidence that scan latency is user-visible, and no
  profiling was done. Still deferred, not rejected outright — re-audit
  if profiling ever shows scan latency as a problem.
- **Multi-tab counter race** (`content.js:980-983` comment): accepted by
  design (incidence low, impact cosmetic). Not re-planned.
- **Per-pattern stats** is a direction finding (041), not a table
  finding — the popup already shows attribution in the undo window; the
  gap is persistence + a UI, which is a product decision, so it ships as
  a design plan.
